name: Verify Commit

on:
  push:
  pull_request:

jobs:
  check:
    name: 🔐 Notarized GPG Proof Check
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Dummy GPG check
        run: |
          echo "✔️ GPG Notarization Placeholder"
          echo "Everything passed!"
